{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["public/js/bundled/**"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }],
  "buildCommand": "pnpm run build:js"
}
